/**
 * Creates a new Coordenates
 * @class
 */
export default class Coordenates {
  /** 
   * @constructor
   * @param {number} x
   * @param {number} y
   */
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
};